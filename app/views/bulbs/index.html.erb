<p id="notice"><%= notice %></p>

<h1>Bulbs</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Parent</th>
      <th>Color</th>
      <th>Parent type</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @bulbs.each do |bulb| %>
      <tr>
        <td><%= bulb.user_id %></td>
        <td><%= bulb.parent_id %></td>
        <td><%= bulb.color %></td>
        <td><%= bulb.parent_type %></td>
        <td><%= link_to 'Show', bulb %></td>
        <td><%= link_to 'Edit', edit_bulb_path(bulb) %></td>
        <td><%= link_to 'Destroy', bulb, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Bulb', new_bulb_path %>
